# Gradle
.gradle/
build/

# IDE
.idea/
*.iml
*.ipr
*.iws
out/
.vscode/
bin/

# Minecraft dev run directories (Fabric / Bukkit)
run/
run-client/
run-server/

# Local runtime data (H2 database, content-addressed blob store)
data/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Frontend build tooling: Tailwind standalone CLI binary (~125 MB; downloaded per machine).
# The built CSS (static/css/app.css) and the Tailwind input are committed; the binary is not.
web/tools/

# Local dev helper (machine-specific paths)
run-test-client.cmd
